# 🎥 Быстрая настройка для загрузки видео 650MB+

## ✅ Что уже сделано в коде

1. **Next.js** - лимит увеличен до 1GB (`next.config.ts`)
2. **API роут** - таймаут 5 минут (`api/upload/route.ts`)
3. **UI** - обновлена подсказка "до 1GB"
4. **Vercel config** - создан `vercel.json` (если используете Vercel)

## 🚨 ВАЖНО: Настройка хостинга

### Вариант А: Если у вас VPS/Dedicated сервер

#### 1. Nginx
```bash
# Отредактируйте конфиг
sudo nano /etc/nginx/nginx.conf

# Добавьте в http блок:
client_max_body_size 1024M;
client_body_timeout 300s;
client_header_timeout 300s;

# Проверьте и перезапустите
sudo nginx -t
sudo systemctl reload nginx
```

#### 2. PM2 (если используете)
```bash
# Запустите с увеличенной памятью
NODE_OPTIONS="--max-old-space-size=4096" pm2 start npm --name "kinosite" -- start
```

### Вариант Б: Если используете Vercel/Netlify

**ПРОБЛЕМА:** Vercel Hobby/Pro план имеет лимит 50-100MB для загрузки файлов.

**РЕШЕНИЯ:**

#### Решение 1: Cloudflare R2 (рекомендуется, почти бесплатно)
1. Создайте аккаунт на Cloudflare
2. Включите R2 (10GB бесплатно в месяц)
3. Загрузите видео через их интерфейс
4. Получите публичную ссылку
5. Используйте поле "URL видео" в админке

#### Решение 2: AWS S3 / DigitalOcean Spaces
1. Создайте bucket
2. Загрузите видео
3. Сделайте файл публичным
4. Используйте URL в админке

#### Решение 3: Перейдите на VPS
Hetzner/DigitalOcean VPS от $4-6/месяц без лимитов.

### Вариант В: Если у вас Apache + cPanel

Добавьте в `.htaccess` в корне проекта:
```apache
php_value upload_max_filesize 1024M
php_value post_max_size 1024M
php_value max_execution_time 300
php_value max_input_time 300
php_value memory_limit 512M
```

## 🧪 Тестирование

1. **Локально (должно работать сразу):**
   ```bash
   cd web
   npm run dev
   ```
   Откройте админку и попробуйте загрузить видео.

2. **На хостинге:**
   - Соберите: `npm run build`
   - Задеплойте
   - Попробуйте загрузить

## ❓ Что делать, если не работает?

### Ошибка "Payload too large" (413)
- **Локально:** Перезапустите сервер (`npm run dev`)
- **На хостинге:** Проверьте настройки Nginx/Apache (см. выше)

### Таймаут при загрузке
- Увеличьте таймауты в Nginx (см. `nginx-upload-config.conf`)
- Проверьте скорость интернета

### На Vercel не работает
- Vercel имеет жёсткие лимиты
- Используйте внешнее хранилище (Cloudflare R2, AWS S3)

## 🎬 Рекомендация: Сжать видео

Ваше видео 650MB можно сжать до 200-300MB без потери видимого качества:

### FFmpeg (бесплатно, 1 команда):
```bash
ffmpeg -i input.mp4 -c:v libx264 -crf 23 -preset medium -vf scale=1920:-2 -c:a aac -b:a 128k output.mp4
```

### Онлайн:
- [HandBrake](https://handbrake.fr/) - десктоп приложение
- [CloudConvert](https://cloudconvert.com/) - онлайн

**Результат:** файл станет ~3x меньше, загрузится быстрее, займёт меньше места.

## 📁 Файлы для справки

- `UPLOAD_LARGE_FILES_GUIDE.md` - полная документация
- `nginx-upload-config.conf` - готовый конфиг Nginx
- `vercel.json` - конфиг для Vercel

## 💬 Поддержка

Если застряли, проверьте:
1. Логи сервера (`pm2 logs` или в админке хостинга)
2. Browser DevTools → Network → смотрите ошибку при загрузке
3. Документацию вашего хостинга про file upload limits

